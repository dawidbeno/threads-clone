import { Text, View } from 'react-native';

export default function ProfileScreen() {
    return (
        <View>
            <Text style={{ fontSize: 24, fontWeight: 'bold', textAlign: 'center' }}>Profile Screen</Text>
        </View>
    );
};